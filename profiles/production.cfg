[buildout]
extends = base.cfg

parts = zeo
    instance
    varnish-build
    varnish
    supervisor
    supervisor-conf
    supervisor-crontab
    packcronjob
    backup
    backupcronjob
    logrotate-conf
    logrotate

[instance]
zeo-client = True
zeo-address = ${zeo:zeo-address}
